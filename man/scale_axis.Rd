% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale_axis.R
\name{scale_axis}
\alias{scale_axis}
\title{Scales values for use on a secondary axis in ggplot.}
\usage{
scale_axis(value, first, second)
}
\arguments{
\item{value}{The value requiring rescaling}

\item{first}{List of values that determines the primary axis}

\item{second}{List of values that determines the secondary axis}
}
\value{
Rescaled and transformed values that will fit on the primary axis
}
\description{
This function, combined with \code{\link[=scale_value]{scale_value()}}, makes it easy to use a secondary axis in ggplot. Simply supply the possible values for both axes to rescale values for the secondary axis.
This function calculates the axis labels by shifting the values (subtracting the minimum value of the second set from the minimum of the first set) and rescaling them by the difference in variance between the two sets.
Simply put: \verb{axis value = shift - value * scale * -1}
}
\examples{
library(ggplot2)
library(lubridate)
dataset = data.frame(t=seq(ymd("2023-01-01"), ymd("2023-01-10"), 1),
                     primary=1:10,
                     secondary=log(1:10))
ggplot(dataset) +
  geom_point(aes(x=t, y=primary)) +
  geom_line(aes(x=t, y=scale_value(secondary, primary, secondary))) +
  scale_y_continuous(sec.axis=sec_axis(~scale_axis(., dataset$primary, dataset$secondary),
                                       name="Log"))

}
\seealso{
\code{\link[=scale_value]{scale_value()}}, \code{\link[=sec_axis]{sec_axis()}}
}
